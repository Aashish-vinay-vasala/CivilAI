apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: civilai
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: us-central1-docker.pkg.dev/gen-lang-client-0881995245/civilai/backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: GROQ_API_KEY
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: GEMINI_API_KEY
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: SUPABASE_URL
            - name: SUPABASE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: SUPABASE_SECRET_KEY
            - name: HUGGINGFACE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: HUGGINGFACE_TOKEN
            - name: RESEND_API_KEY
              valueFrom:
                secretKeyRef:
                  name: civilai-secrets
                  key: RESEND_API_KEY
                  optional: true # app degrades gracefully (logs instead of sending) when unset
            - name: ALLOWED_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: civilai-config
                  key: ALLOWED_ORIGINS
            - name: REDIS_URL
              value: "redis://redis:6379/0"
          resources:
            requests:
              cpu: "500m"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "6Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: civilai
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
