# Placeholder only — DO NOT commit real values here.
# `stringData` takes plain text (kubectl/kubernetes base64-encodes it for you),
# so this file can be applied as-is for a smoke test without crash-looping,
# then overwritten for real via `kubectl create secret --dry-run=client -o yaml`,
# a sealed-secrets/External Secrets Operator pipeline, or the Secret Manager CSI
# driver — never by editing real credentials into a file that gets committed.
apiVersion: v1
kind: Secret
metadata:
  name: civilai-secrets
  namespace: civilai
type: Opaque
stringData:
  GROQ_API_KEY: REPLACE_ME
  GEMINI_API_KEY: REPLACE_ME
  SUPABASE_URL: REPLACE_ME
  SUPABASE_SECRET_KEY: REPLACE_ME
  HUGGINGFACE_TOKEN: REPLACE_ME
  RESEND_API_KEY: REPLACE_ME
